---
description: Core project architecture and conventions for bitkaisen
alwaysApply: true
---

# bitkaisen - Project Overview

BitLife-style Jujutsu Kaisen life simulator. **Rojo** is the source of truth -- all game code lives in this repo, synced to Studio via `rojo serve`.

## Game Features

Life simulation with JJK theming: aging, clans, cursed techniques, technique mastery, traits, missions, relationships, civilian jobs, combat, domain expansion, binding vows, rivals, achievements, daily login rewards, and world history progression through Heian/modern eras.

## Tech Stack

- **Language**: Luau (`.luau` only, never `.lua`)
- **Sync**: Rojo (`game.project.json`)
- **Networking**: Blink (codegen from `.blink` definitions) for RemoteEvent/RemoteFunction; BK_Remotes module for classic remotes
- **UI**: Vide (Bravo's fork) -- reactive component tree, single `vide.mount()` bootstrap replaces ~40 imperative scripts. Fusion 0.2 is installed for a future Hydrate migration.
- **Scaling**: Vide `px.luau` (getter-based) -- design at 1920×1080, logarithmic scaling to all devices. Legacy Fusion `px.luau` remains in `global/shared/utils/`.
- **Data**: ProfileStore + Charm (atoms/signals) + Delta-Compress (replication diffs)
- **Validation**: GreenTea schemas
- **Build**: Darklua + Selene (lint) + Rokit (toolchain) + Pesde/Wally (packages)
- **Task runner**: `just` (see `justfile`)

## Architecture

The game uses a traditional Roblox scripting architecture (not ECS):
- **Server**: `GameManager.server.luau` is the central server script orchestrating all `BK_*` service modules
- **Client**: `BitKaisenClient/init.client.luau` bootstraps the UI. It mounts the Vide reactive shell (ProfileCard, LifeLog, NavPanel, AgeUpButton, SettingsButton, MajorPopup, NotificationHolder) via `vide.mount()`, then initializes all panel modules
- **Panels**: Each panel is a standalone module at `game/shared/ui/panels/` that owns its own ScreenGui, uses `Instance.new()` for all UI construction, and wires to `uiState.subscribe()` for route-driven open/close or listens for remote events for event-driven panels. Panels use imperative `refresh()` methods and `:Connect()` for events -- no Vide reactive primitives
- **State**: `playerState` (reactive source from `UpdateLifeEvent`), `uiState` (route/modal stack + imperative `subscribe(fn)->disconnect` observers for panels), `navRegistry` (dynamic nav entries), `remotes` (centralized remote access)
- **Controllers**: `TopBarController`, `SFXController`, `ConsoleSupport` provide cross-cutting concerns (top bar layout, sound effects, gamepad support)

## Folder Layout

```
game/server/                -> ServerScriptService (GameManager, BK_* services)
game/server/BitKaisenServer/ -> Server service modules (BK_Remotes, BK_CombatService, etc.)
game/shared/                -> ReplicatedStorage (hooks, controllers, config)
game/shared/ui/             -> UI tree: shell components (Vide), panels (imperative modules), state/, hooks/, theme, helpers, px
game/shared/ui/panels/      -> All panel modules (30+), each with own ScreenGui, imperative Instance.new() code
game/shared/ui/components/  -> Vide reactive shell components (ProfileCard, LifeLog, NavPanel, etc.)
game/shared/ui/state/       -> Shared state: playerState, uiState (with subscribe), navRegistry, remotes
game/starterplayer/         -> StarterPlayerScripts (BitKaisenClient bootstrap, MobileOrientationLock, GroupJoin)
global/server/              -> ServerScriptService/global (accountService, commands)
global/shared/              -> ReplicatedStorage/global (types, utils, constants, controllers)
global/client/              -> StarterPlayerScripts/global
remotes/                    -> Blink .blink definitions (codegen output in remotes/out/, git-ignored)
```

## Key Commands

- `just packages` -- install deps + generate sourcemap
- `just dev` -- start dev (rojo serve + blink watch)
- `just net` -- compile blink networking only
